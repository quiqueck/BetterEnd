# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx8G
#Loom
loom_version=1.10-SNAPSHOT
#Modrinth
modrinth_versions=["1.21.6", "1.21.7", "1.21.8"]
#CurseForge
curseforge_id=413596
#Dependencies
required_dependencies=["fabric-api", "worldweaver", "bclib"]
#optional_dependencies=["patchouli", "trinkets", "emi", "modmenu"]
optional_dependencies=[ "modmenu"]
# Parent of the FIRST commit on release/1.21.6, so it reads as a diff instead of a
# full import; later releases sit on the previous release commit. Everything
# reachable from this ref becomes part of the public release branch, which is why
# this is origin/1.21.6 - the already-published tip - and not the local branch: the
# local-only 1.21.6 commits then ship as content in one release commit rather than
# as individual commits with their own messages.
previous_release_branch=origin/1.21.6
#`release`, `beta` or `alpha`
release_channel=beta
modrinth_id=gc8OEnCC
mod_id=betterend
# Fabric Properties
# check these on https://fabricmc.net/versions.html
minecraft_version=1.21.8
loader_version=0.16.14
fabric_version=0.130.0+1.21.8
# Mod Properties
mod_version=21.800.0
maven_group=org.betterx
archives_base_name=better-end
# Dependencies
bclib_version=21.800.0
wover_version=21.800.0
wunderlib_version=21.800.0
patchouli_version=1.19-73-FABRIC
wthit_version=16.0.4
rei_version=16.0.744
emi_version=1.1.8+1.21
trinkets_version=3.10.0
cca_version=6.1.1
modmenu_version=15.0.2